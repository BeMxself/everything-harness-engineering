---
import { LinkButton } from "@astrojs/starlight/components";
---

<section id="definition" class="overview-section overview-section--definition">
  <div class="overview-grid">
    <div class="overview-prose">
      <p class="section-label">概念入口</p>
      <h2>先把 AI Agents、Agent Harnesses 和 Harness Engineering 的边界说清楚。</h2>
      <p class="lead">
        <strong>Harness Engineering</strong> 讨论的是人如何站在 <strong>AI Agents</strong> 与
        <strong>Agent Harnesses</strong> 提供的工作面之上，把命令、MCP、钩子、技能、子代理、记忆和审批机制组织成可持续、可控、可验证的开发系统。
      </p>
      <p>
        更稳的理解方式是先分开三件事：<code>AI Agents</code> 提供主执行宿主，
        <code>Agent Harnesses</code> 在其上补控制面、恢复面与用户参与契约，
        <code>命令 / MCP / 钩子 / 技能 / 子代理 / 记忆 / 审批</code> 是这些系统调用和编排的机制层，
        <code>Harness Engineering</code> 则是人如何把这些能力编成长期开发方法。
      </p>
    </div>

    <aside id="boundary" class="boundary-panel not-content">
      <p class="section-label">先抓这三个问题</p>
      <ul>
        <li><code>Agent Harnesses</code> 这一层到底对应哪些系统。</li>
        <li><code>命令 / MCP / 钩子 / 技能 / 子代理</code> 这些机制到底由谁提供、由谁编排。</li>
        <li><code>Harness Engineering</code> 为什么是实践视角，不是第四类产品。</li>
      </ul>
      <LinkButton href="/everything-harness-engineering/concept-entry/" variant="secondary" icon="right-arrow">
        进入概念入口
      </LinkButton>
    </aside>
  </div>
</section>
