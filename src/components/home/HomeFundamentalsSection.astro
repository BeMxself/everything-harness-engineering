---
import { LinkButton } from "@astrojs/starlight/components";

interface FundamentalItem {
  title: string;
  summary: string;
}

interface Props {
  items: FundamentalItem[];
}

const { items } = Astro.props;
---

<section id="fundamentals" class="overview-section">
  <div class="overview-section-head">
    <div class="overview-prose">
      <p class="section-label">基本构成</p>
      <h2>要理解 harness engineering，先要看清 agent harness 提供哪些底层能力。</h2>
      <p>
        它不是一个单独功能点，也不是另一个产品类，而是建立在一组外层系统能力之上的工程实践。下面这些项更适合先看成
        <strong>harness 机制</strong>：它们不都等于 agent harness 本身，而是 agent harness 暴露出来、也被
        harness engineering 用来驾驭 AI 做开发的关键控制面。
      </p>
    </div>
    <div class="section-actions not-content">
      <LinkButton href="/everything-harness-engineering/concept-entry/" variant="minimal" icon="right-arrow">
        去看完整构成说明
      </LinkButton>
    </div>
  </div>

  <div class="fundamentals-grid not-content">
    {items.map((item) => (
      <article class="fundamental-card">
        <h3>{item.title}</h3>
        <p>{item.summary}</p>
      </article>
    ))}
  </div>
</section>
