---
import { LinkButton } from "@astrojs/starlight/components";

interface TaxonomyItem {
  title: string;
  description: string;
  note: string;
  href: string;
}

interface Props {
  items: TaxonomyItem[];
}

const { items } = Astro.props;
---

<section id="taxonomy" class="overview-section">
  <div class="overview-section-head">
    <div class="overview-prose">
      <p class="section-label">分类</p>
      <h2>如果不先分清类型，后面的比较很容易全都混在一起。</h2>
      <p>
        这一页里的套件看起来都和 agent 有关，但它们处理的问题并不在同一层。
        有些是在提供原生工作面，有些是在宿主之上补控制层，有些则更偏方法论或长时编排。
        先把它们放回各自的位置，后面的分析、对比和选择才会变得清楚。
      </p>
      <p>
        这组分类的目的，不是替每个项目发明新的官方身份，而是帮助读者先判断自己正在比较哪一层：
        是直接执行工程任务的 agent 宿主，是覆盖在其上的控制层，还是方法论与编排层。
        只有先分清层次，后面的比较才不会把不同问题混成同一种产品选择。
      </p>
    </div>
    <LinkButton href="./distinction/" variant="minimal" icon="right-arrow">
      查看完整分类
    </LinkButton>
  </div>

  <div class="taxonomy-grid not-content">
    {items.map((item) => (
      <article class="taxonomy-card">
        <h3>{item.title}</h3>
        <p>{item.description}</p>
        <p class="taxonomy-note">{item.note}</p>
        <a href={item.href}>查看完整分类</a>
      </article>
    ))}
  </div>
</section>
