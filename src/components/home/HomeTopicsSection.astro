---
import { CardGrid, LinkButton, LinkCard } from "@astrojs/starlight/components";

interface TopicItem {
  title: string;
  description: string;
  href: string;
}

interface Props {
  items: TopicItem[];
}

const { items } = Astro.props;
---

<section id="topics" class="overview-section">
  <div class="overview-section-head">
    <div class="overview-prose">
      <p class="section-label">研究专题</p>
      <h2>要真正理解这个课题，还需要进入可运行专题。</h2>
      <p>
        现在的专题页不只是入口，还会说明 topic 应该回答什么、当前已经能回答什么、以及还不能回答什么。
        首页在这里只负责把你送进那个更具体的层次。
      </p>
    </div>
    <div class="section-actions not-content">
      <LinkButton href="/everything-harness-engineering/topics/" variant="minimal" icon="right-arrow">
        去看专题说明页
      </LinkButton>
    </div>
  </div>

  <CardGrid>
    {items.map((topic) => (
      <LinkCard title={topic.title} description={topic.description} href={topic.href} />
    ))}
  </CardGrid>
</section>
