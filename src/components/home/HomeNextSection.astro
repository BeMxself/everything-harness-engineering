---
import { CardGrid, LinkCard } from "@astrojs/starlight/components";

interface NextLink {
  title: string;
  description: string;
  href: string;
}

interface Props {
  items: NextLink[];
}

const { items } = Astro.props;
---

<section id="next" class="overview-section overview-section--next">
  <div class="overview-prose">
    <p class="section-label">下一步</p>
    <h2>如果首页已经让你对整体图景有了感觉，下一步就按问题深入。</h2>
    <p>
      这几条入口对应的是几种完全不同的阅读目标：定义边界、理解形成过程、比较系统路线，或者直接进入可运行材料。
    </p>
  </div>

  <CardGrid>
    {items.map((item) => (
      <LinkCard title={item.title} description={item.description} href={item.href} />
    ))}
  </CardGrid>
</section>
